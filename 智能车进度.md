# 智能车开发进度

## 文档规范
- 文件编码：UTF-8 with BOM
- 时间格式：YYYY-MM-DD HH:mm（UTC+8）
- 记录原则：仅保留当前有效阶段与最近关键里程碑

## 进度记录（精简版）
| 序号 | 时间 | 阶段 | 目标 | 关键改动 | 变量/开关状态 | 验收结果 | 备注/下一步 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 41 | 2026-03-23 15:44 | 阶段7-双PD与固定周期控制收敛 | 完成直道/弯道双参数切换与连续弯稳定性收敛 | 控制执行改为固定周期；直道/弯道双参数集与迟滞切换收敛，串口新增 `CM`，LCD显示 `CM` | `ENABLE_STAGE5_GATE_CONTROL=1`，`SERVO=1`，`MOTOR=1` | 通过 | 为后续代码模块化与元素识别打底 |

## 交接快照（当前有效）
- 主文件：`project/user/main_cm7_0.c`（调度主循环 + 显示/串口）。
- 电机输出：`project/user/cm7_0_isr.c`。
- 核心模块：
  - `project/code/WhiteSeed.c/.h`（阶段1：动态阈值 + 最长白列）
  - `project/code/SearchLine.c/.h`（阶段2：找边、中线、十字状态机）
  - `project/code/MiddlineError.c/.h`（阶段3：误差与滤波）
  - `project/code/ServoPID.c/.h`（阶段4：舵机/速度控制）
  - `project/code/MotorPID.c/.h`（电机PID）
  - `project/code/TrackTypes.h`（共享类型）
- 当前串口主字段：`[OBS] X Q VR RR CS EF DE DT SA SP CM ES`。

## 编码注意事项（强制）
- 代码文件（`.c/.h`）与文档（`.md`）统一使用：`UTF-8 with BOM`。
- 禁止在同一工程混用 `GBK/ANSI/UTF-8`。
- 若发现中文注释乱码，先修复编码再改内容。
